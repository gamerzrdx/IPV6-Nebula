# IPV6 NEBULA INSTALLION

## Installation

```bash
# Clone the repository
git clone https://github.com/gamerzrdx/IPV6-Nebula.git
cd IPV6-Nebula
mv nebula.blueprint /var/www/pterodactyl
cd /var/www/pterodactyl
blueprint -install nebula
